{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm install puppeteer --no-optional",
    "postinstall": "apt-get update && apt-get install -y chromium",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1",
    "puppeteer": "^20.8.1",
    "puppeteer-core": "^20.8.1",
    "readline": "^1.3.0",
    
  }
}
